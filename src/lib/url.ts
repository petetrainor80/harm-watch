// URL normalisation library — implemented in M3.
// See PRD section 9.2 for the full algorithm.

export function normaliseUrl(_raw: string): { normalised: string; domain: string; hash: string } {
  throw new Error("Not implemented — build in M3");
}
